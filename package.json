{
    "name": "disk-db",
    "version": "0.2.0",
    "description": "disk-db",
    "license": "MIT",
    "main": "build/index",
    "types": "build/index",
    "files": [
        "build"
    ],
    "scripts": {
        "prebuild": "npm run clean && npm run format && npm run lint && echo Using TypeScript && tsc --version",
        "build": "tsc --pretty",
        "clean": "rimraf build && rimraf coverage",
        "coverage": "jest --coverage",
        "format": "prettier --write \"{src,__tests__}/**/*.ts\" --single-quote --trailing-comma es5",
        "lint": "tslint --fix --force --format verbose \"src/**/*.ts\"",
        "majestic": "majestic",
        "prepublishOnly": "npm run build",
        "test": "jest",
        "watch": "DEBUG=diskdb; npm run build -- --watch",
        "watch:test": "npm run clean && DEBUG=diskdb; jest --watch"
    },
    "jest": {
        "collectCoverage": true,
        "coverageThreshold": {
            "global": {
                "branches": 50,
                "functions": 50,
                "lines": 50,
                "statements": 50
            }
        },
        "notify": false,
        "preset": "ts-jest",
        "testPathIgnorePatterns": [
            ".history/"
        ],
        "verbose": false
    },
    "dependencies": {
        "chalk": "^4.1.0",
        "debug": "^4.2.0",
        "majestic": "^1.7.0"
    },
    "devDependencies": {
        "@types/debug": "^4.1.5",
        "@types/jest": "^26.0.14",
        "@types/node": "^14.11.1",
        "coveralls": "^3.0.2",
        "jest": "^26.4.2",
        "prettier": "^2.1.2",
        "rimraf": "^3.0.2",
        "ts-jest": "^26.3.0",
        "ts-node": "^9.0.0",
        "tslint": "^6.1.3",
        "tslint-config-prettier": "^1.15.0",
        "typescript": "^4.0.3"
    },
    "engines": {
        "node": ">=10.0.0"
    }
}
